#include <SoftwareSerial.h>

SoftwareSerial virtuelSerialLora(9,10); //RX/TX

int r = 0;
String rs = "";

void setup() {
  // put your setup code here, to run once:
  virtuelSerialLora.begin(19200);
  Serial.begin(19200);
}

void loop() {
  // put your main code here, to run repeatedly:

  if(virtuelSerialLora.available()){
    Serial.write(virtuelSerialLora.read());
      //r = virtuelSerialLora.read();
      //rs += (char)r;
      //if(!virtuelSerialLora.available()){
      // Serial.print(rs); 
      // rs = "";
      //}
      
  }
  if(Serial.available()){
    virtuelSerialLora.write(Serial.read());
    //virtuelSerialLora.print("A");
  }

}